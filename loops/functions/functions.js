//function declaration
/* function one() {
        return 'one'
}
one(); //function expression
const two = () =>{
    return 2;
}
const shortenedTwo = () => 2;

function areBothEven(n1,n2) {
    return !(n1 % 2) && !(n2 % 2);
}

console.log(areBothEven(3,7)); */

const areaOfTriangle (base, height) => {
    areaOfTriangle = base * height/2
    console.log('The area of a triangle with a width of ${base} and length of ${height} is ${areaOfTriangle} square units.')
}

areaOfTriangle(5,2);